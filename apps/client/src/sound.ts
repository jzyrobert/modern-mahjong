/**
 * Phase 7 will swap these stubs out for `expo-audio` Sound objects
 * loaded from pre-rendered .wav assets (the Web Audio API procedural
 * synthesis from the legacy build doesn't translate to RN). For now
 * these are no-ops so the orchestrator can call them without crashing.
 */

export function playClick(): void {
  // TODO Phase 7
}

export function playDiscard(): void {
  // TODO Phase 7
}

export function playWinFanfare(): void {
  // TODO Phase 7
}
