/**
 * PLACEHOLDER — the menu feature agent replaces this with the real
 * MenuScene hero backdrop (ARCHITECTURE.md §1 `menu/`). Must stay a
 * zero-prop component that fills its positioned parent.
 */
export function Menu3DBackdrop() {
  return null;
}
