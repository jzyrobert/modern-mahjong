node_modules
dist
build
.cache
*.tsbuildinfo
.turbo
.parcel-cache
coverage
*.log
.DS_Store
.env
.env.local
.wrangler
.dev.vars

# Native shells — regenerated by `npx expo prebuild` in CI. Match
# only the top-level directories so the local Expo modules under
# `apps/client/modules/<name>/{ios,android}/` (which DO need to be
# checked in) aren't swept up by these rules.
/apps/client/ios
/apps/client/android

# Playwright outputs
apps/client/test-results
apps/client/playwright-report

# Lighthouse CI outputs
apps/client/.lighthouseci

# IDE
.idea
.vscode
*.iml

# OS
Thumbs.db

# Claude Code per-user settings
.claude/
